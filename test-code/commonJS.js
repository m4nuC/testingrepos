/**
 * Dispatch a request to the server using the configured adapter.
 * second line () with *3 Weird 75% $# symbols!~`.
 *
 * @param {Type} withType This one has a type.
 * @param noType This one does not have a type.
 * @returns {Promise} The Promise to be fulfilled
 */
module.exports = function dispatchRequest(config) {
  var iAmAVriable = "Pouette pouette";

  return "yeah";
};

/**
 * @returns Just a hard coded string.
 */
module.exports = function(config) {
  var iAmAVriable = "Pouette pouette";

  return "yeah";
};

// Just some hand written comment
module.exports = function inlineComment(config) {
  var iAmAVriable = "Pouette pouette";

  return "yeah";
};
